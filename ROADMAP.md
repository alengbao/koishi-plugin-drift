# Drift 开发计划

本文档记录 Drift 后续开发方向。完成一个阶段后，将对应的 `[ ]` 改为
`[x]`，并在提交信息中引用本计划。内容配置和代码实现必须同时通过测试后，
才能标记为完成。

## 已完成基础

- [x] JSON 内容源码、bundle 构建和 SQLite 发布快照
- [x] 内容版本、外部覆盖、检查、热加载、发布和导出流程
- [x] 条件探索事件、次数、冷却、权重和事件选项快照
- [x] 测试模式与角色重置、发放物品、设置 HP/AP、强制事件
- [x] 森林的基础资源、食物、工具、敌人、建筑和探索事件

## 第一阶段：生存内容

- [x] 增加肉、鱼、果实、蘑菇等食物内容
- [x] 将食物库存改为批次库存，记录获得时间和过期时间
- [x] 食物自动按最早过期优先消耗，永久保存食物最后消耗
- [x] 过期食物采用惰性清理，不引入后台定时任务
- [ ] 新增熏制等处理配方，支持将易腐食物转为长期保存食物
- [ ] 扩充森林事件、资源、工具和基础建筑

食物配置至少支持：

```text
shelfLifeDays: number | null
nutrition: number
```

口粮等长期保存食物使用 `shelfLifeDays: null`。鲜鱼、生肉、野生菌和野果
分别保存 2、3、4、6 个日历日；获得日为第 0 天，过期日 00:00 起不可食用。
食物库存按批次记录获得日和过期日，并在查看背包或执行有效行动时惰性删除
腐坏批次。每天第一次有效行动自动消耗最早过期的食物，长期保存食物最后
消耗；所有内置食物本阶段提供 1 点营养。肉、鱼和野果暂时通过测试命令发放，
野生菌继续由既有探索事件产生。现有木材、石块等非食物资源继续使用普通
数量库存。

## 第二阶段：角色独立地图与地点

- [x] 为每个角色生成并持久化独立 `mapSeed`
- [x] 新增通用地点内容类型，并加入河流和岩洞
- [x] 新增角色地图与地点实例表，保存环形位置、发现状态和互动状态
- [x] 根据地区配置和角色种子生成不同数量、种类和顺序的地点
- [x] 地图生成后保持稳定，重启和内容更新不会重新随机
- [x] 地点发现和地点互动接入现有行动事务
- [ ] 增加地点专属事件内容并接入地点互动

同一地区不保证拥有相同地图。两个森林角色可以看到不同数量和类型的地点。
地点类型是内容定义，地点实例是角色地图中的实际地点。同一环节可以保存多个
地点实例；当前 Bot 界面只按发现顺序显示地点，不暴露环形位置。

探索先以固定事件权重 5 和可增长的地点权重 1 进行二选一。每次触发事件后
地点权重增加 1，发现地点后重置为 1。探索积累不向玩家显示。

初期地图使用 7 节环：

```text
0 -- 1 -- 2 -- 3
|              |
6 -- 5 -- 4
```

环上的距离为 `min(abs(a - b), 7 - abs(a - b))`，最大距离为 3。

## 第三阶段：定居与一日通行证

- [ ] 允许角色在符合条件的地点定居
- [ ] 庇护所和后续建筑绑定到地点实例，而不是只绑定地区
- [ ] 定居后最大行动点增加 3：基础 3 + 定居加成 3
- [ ] 记录定居地点和定居建筑等级
- [ ] 增加一日通行证，记录当天可访问的最大距离档位
- [ ] 访问地点时检查通行证范围和剩余行动点
- [ ] 首次访问或扩大范围时自动购买、升级通行证并扣除费用
- [ ] 通行证按 Asia/Shanghai 日期惰性失效
- [ ] 不记录玩家当前身处位置，地点访问不产生移动状态

通行证只对已定居角色生效。未定居角色访问地点不收取距离费用。

距离档位和费用固定为：

| 档位 | 最大距离 | 通行证费用 |
| --- | ---: | ---: |
| 临近 | 0 | 0 AP |
| 近 | 1 | 1 AP |
| 远 | 2 | 2 AP |
| 极远 | 3 | 3 AP |

购买通行证后，当天可以访问该距离及以内的所有地点，重复访问不重复扣除
通行证费用。地点本身的互动仍按正常行动消耗 AP。通行证购买和地点互动必须
在同一事务内检查，AP 不足时不产生任何部分修改。

玩家不需要单独输入购买命令。执行地点动作时，系统自动比较目标距离和当天
已有通行证：没有通行证时支付目标档位的完整费用；已有较低档位时只支付升级
差价。例如当天已经支付 1 AP 到达“近”，之后访问“远”只再支付 1 AP。
自动扣费必须在行动反馈中明确显示。

## 第四阶段：自动化卡片战斗

- [ ] 抽象独立于 Koishi 的战斗引擎
- [ ] 玩家和敌人拥有数据化卡组
- [ ] 实现洗牌、抽牌、回合和自动结算
- [ ] 实现基础攻击、防御和重击卡
- [ ] 战斗前允许玩家选择卡组或战斗策略
- [ ] 保存随机种子和战斗日志，保证战斗结果可复现
- [ ] 在基础规则稳定后增加先制、响应和反应机制
- [ ] 将现有自动战斗逐步迁移到卡片战斗引擎

战斗中不要求玩家逐回合输入。交互集中在战斗开始前的卡组、装备和策略选择。

## 第五阶段：种族、职业与装备

- [ ] 将种族从固定 `human` 扩展为内容配置
- [ ] 将职业从固定 `drifter` 扩展为内容配置
- [ ] 增加装备槽位和装备实例
- [ ] 支持装备提供属性、能力标签和卡牌修改
- [ ] 支持种族、职业和装备组合生活技能与战斗技能
- [ ] 将属性加成统一为可组合的声明式效果

这一阶段应与卡片战斗引擎的属性和能力接口保持一致，避免分别实现两套
技能系统。

## 第六阶段：更多地形

- [ ] 增加沼泽
- [ ] 增加海岸或海洋
- [ ] 增加沙漠
- [ ] 为新地形配置独立资源池、地点池、事件池和定居限制
- [ ] 验证新增地形不需要修改核心行动和事务流程

新地形应主要通过 JSON 内容扩展，核心代码只负责通用规则。

## 实现约束

- [ ] 所有玩家数据写入继续使用事务和 `requestId` 幂等日志
- [ ] 地点、食物和战斗规则必须可通过核心服务调用，不能依赖 Koishi `Session`
- [ ] 内容引用继续使用稳定的 `(type, contentId)`，不使用雪花 ID
- [ ] 已发布内容更新遵循版本递增，不覆盖玩家数据
- [ ] 每个阶段完成后补充单元测试、SQLite 集成测试和沙盒验收
